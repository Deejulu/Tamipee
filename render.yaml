services:
  - type: web
    name: tamipee
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn tamipee.wsgi:application --bind 0.0.0.0:$PORT
