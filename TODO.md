# TODO

- [ ] Update `requirements.txt` to include missing dependency `reportlab`.
- [ ] Redeploy so Render installs the new dependency.
- [ ] Quick sanity check: confirm `download_credentials_pdf` no longer throws `ModuleNotFoundError`.
- [ ] (Optional) Redeploy and verify PDF endpoint works.


